version: "2"
services:
  node:
    image: "node:17.0.1"
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/home/node/app
    ports:
      - "4000:4000"
    command: "npm run dev"