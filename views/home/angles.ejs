<%-include('../includes/header.ejs')%>

<div class="p-10">
    <div class="py-2 mt-7">
        <h1 class="text-2xl font-poppins">Vilken lutning har ditt tak?</h1>
    </div>
    
    <form action="/angles" method="post">
        <% angles.forEach(angle => { %>
            <div class="flex flex-row items-center p-5">
                <input id="angle-<%= angle.value %>" type="radio" name="angle" value="<%= angle.value %>" class="h-8 w-8">
                <label for="angle-<%= angle.value %>" class="text-xl ml-5"><%= angle.label %>&deg</label>
            </div>
        <% }); %>
        <input type="hidden" name="_csrf" value="<%= csrfToken%>">
        <div>
            <button type="submit" class="rounded mt-10 bg-green-500 py-4 help px-7 uppercase text-white">NÄSTA</button>
        </div>
    </form>
</div>

<%-include('../includes/help.ejs')%>
<div class="md:p-20 p-5 bg-gray-300 mt-20 bottom-0 mx-2">
    <%-include('../includes/newsletter.ejs')%>
        <%-include('../includes/bottomNav.ejs')%>
</div>

<%-include('../includes/footer.ejs')%>